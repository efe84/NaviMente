namespace NaviMente.WebApi.Infrastructure.Services.Email
{
    public static class HttpResponseExtensions
    {
        //public static string ErrorCode(this aldaba.HttpResponse response)
        //    => response.Error?.GetValue("errorCode")?.ToString() ?? "";

        //public static string Message(this aldaba.HttpResponse response)
        //    => response.Error?.GetValue("message")?.ToString() ?? "";

        //public static string ErrorMessage(this aldaba.HttpResponse response)
        //    => $"{response.ErrorCode} {response.Message}";
    }
}
