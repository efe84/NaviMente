using NaviMente.Tests.Domain.Helpers;
using NLayeredApi._1Template.Tests.Domain.Extensions;

namespace NaviMente.Tests.Domain.Factories.ProcesosSeleccion
{
    internal class CandidatoFactory
    {
        //internal static Candidato CreateInstance (long id, Nacionalidad? nacionalidad = default, DateTime? fechaValidezPermisoTrabajo = default)
        //{
        //    Candidato? candidato = EntityHelper.CreateEntity<Candidato>();
        //    if (candidato is null)
        //        throw new Exception("Nullable instance of Candidato");

        //    candidato.SetProperty("Id", id);

        //    if (nacionalidad is null)
        //        nacionalidad = NacionalidadFactory.CreateInstance(1, "ES");
        //    candidato.SetProperty("Nacionalidad", nacionalidad);

        //    if (fechaValidezPermisoTrabajo is not null)
        //        candidato.SetProperty("FechaValidezPermisoTrabajo", fechaValidezPermisoTrabajo);

        //    return candidato;
        //}
    }
}
