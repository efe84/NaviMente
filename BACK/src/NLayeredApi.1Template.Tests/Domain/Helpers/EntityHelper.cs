using System.Reflection;

namespace NaviMente.Tests.Domain.Helpers
{
    internal class EntityHelper
    {
        //internal static T? CreateEntity<T>(params object[] args) where T : Entity
        //    => Activator.CreateInstance(typeof(T), BindingFlags.Instance | BindingFlags.NonPublic, null, args, null, null) as T;
    }
}
